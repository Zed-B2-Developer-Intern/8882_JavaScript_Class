const arr = [2, 3, 4, 5]

//In JS, the arrays are not need to be static sized

//It is similar to 'ArrayList' in java

arr.push(4, 5, 6)               //elements appended

arr.pop()                       //removes and returns the last appended element

console.log(arr);